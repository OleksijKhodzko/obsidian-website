Let $(G, *)$ and $(H, \cdot)$ be two groups, where $*$ is the binary operation for $G$ and $\cdot$ is the binary operation for $H$.

A map $f: G \to H$ is a **group homomorphism** if, for all elements $a$ and $b$ in $G$, the following equation holds:

$$f(a * b) = f(a) \cdot f(b)$$