```python
def sum_v(a, n):
	res = a[0]  # 3
	i = 1  # 2
	while i < n:  # 3n
		res += a[i]  # 5(n - 1)
		i += 1  # 4(n - 1)
	return result  # 2
```

$\large\displaystyle T(n) = 12n - 2 = O(n)$



```python
def s(x, n):
	suma = 0  # 2
	i = 0  # 2
	while i <= n:  # 3(n+2)
		prod = 1  # 2(n+1)
		j = 0  # 2(n+1)
		while j < i:  #  3 * \sum_{i=0}^n (i+1)
			prod *= x  # 5 * \sum_{i=0}^n i
			j += 1  # 4 * \sum_{i=0}^n i
		suma += prod  # 5(n + 1)
		i += 1  # 4(n + 1)
	return suma  # 2
```


$\large\displaystyle T(n) = An^2 + Bn + C$

